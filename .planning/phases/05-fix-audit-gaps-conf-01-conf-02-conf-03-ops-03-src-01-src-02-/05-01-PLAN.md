---
phase: 05-fix-audit-gaps-conf-01-conf-02-conf-03-ops-03-src-01-src-02-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/reconcile_plan.go
  - internal/runtime/reconcile_test.go
  - README.md
  - testdata/config/example.toml
  - testdata/config/socket-proxy.toml
  - .planning/phases/01-runtime-foundation-contracts/01-VERIFICATION.md
  - .planning/phases/03-docker-godoxy-snapshot-automation/03-VERIFICATION.md
  - .planning/phases/04-recovery-observability-deployment/04-VERIFICATION.md
  - .planning/phases/05-fix-audit-gaps-conf-01-conf-02-conf-03-ops-03-src-01-src-02-/05-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - OPS-03
  - SRC-01
  - SRC-02
  - SRC-03
  - RECON-01
  - STATE-01
  - STATE-03
  - STATE-04
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - Restart recovery recreates owned rewrites that are still desired but missing from visible AdGuard state.
    - Restart recovery drops stale owned entries when neither desired state nor visible rewrite remains.
    - Docker deployment docs and sample configs no longer imply a broken `127.0.0.1` AdGuard target for normal container usage.
    - Phase-level verification artifacts exist for Phases 1, 3, and 4 with requirement-level evidence matching the audit gaps.
    - Milestone tracking reflects Phase 5 completion and closes the audit follow-up phase.
  artifacts:
    - path: internal/runtime/reconcile_plan.go
      provides: Recovery-safe plan handling for owned-but-missing visible rewrites.
    - path: internal/runtime/reconcile_test.go
      provides: Targeted coverage for restart recovery create and stale-state cleanup behavior.
    - path: README.md
      provides: Correct container deployment guidance.
    - path: testdata/config/example.toml
      provides: Host-oriented sample config that is explicitly not the default Docker-container path.
    - path: testdata/config/socket-proxy.toml
      provides: Container-friendly network example without localhost AdGuard defaults.
    - path: .planning/phases/01-runtime-foundation-contracts/01-VERIFICATION.md
      provides: Phase 1 requirement verification evidence.
    - path: .planning/phases/03-docker-godoxy-snapshot-automation/03-VERIFICATION.md
      provides: Phase 3 requirement verification evidence.
    - path: .planning/phases/04-recovery-observability-deployment/04-VERIFICATION.md
      provides: Phase 4 requirement verification evidence including the recovery fix and deployment-doc closure.
---

## Phase Goal

**As an** operator, **I want to** close the milestone audit gaps without broadening scope, **so that** the documented v1 milestone is both actually correct and fully evidenced.

<objective>
Close the audit follow-up phase with the smallest set of product, documentation, and verification changes needed for milestone completion.

Purpose: Repair the real recovery defect, remove the deployment-doc warning, and backfill the missing phase verification reports.
Output: Planner fix plus tests, aligned deployment docs/sample configs, verification artifacts for Phases 1/3/4, and updated roadmap/state tracking.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-fix-audit-gaps-conf-01-conf-02-conf-03-ops-03-src-01-src-02-/05-CONTEXT.md
@internal/runtime/reconcile_plan.go
@internal/runtime/reconcile_test.go
@README.md
@testdata/config/example.toml
@testdata/config/socket-proxy.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix restart recovery planner behavior and cover it with tests</name>
  <files>internal/runtime/reconcile_plan.go, internal/runtime/reconcile_test.go</files>
  <acceptance_criteria>
    - Desired records with existing owned lineage are recreated when the visible managed rewrite is missing.
    - Owned records absent from both desired state and visible state are removed from the next managed snapshot.
    - Existing duplicate-visible ambiguity behavior remains non-destructive.
  </acceptance_criteria>
  <action>Adjust `buildReconcilePlan` so owned lineage no longer silently preserves missing-visible state. Add focused tests covering owned-but-missing-visible recovery create and stale owned-state cleanup.</action>
  <verify>
    <automated>mise exec -- go test ./internal/runtime -run 'ReconcilePlanApply|OwnershipBoundary|PreserveManualRecords|RestartRecovery|StaleOwned' -count=1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Close deployment evidence and backfill verification artifacts</name>
  <files>README.md, testdata/config/example.toml, testdata/config/socket-proxy.toml, .planning/phases/01-runtime-foundation-contracts/01-VERIFICATION.md, .planning/phases/03-docker-godoxy-snapshot-automation/03-VERIFICATION.md, .planning/phases/04-recovery-observability-deployment/04-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <acceptance_criteria>
    - Docker deployment docs point operators to a valid container-network example or explicit required override.
    - Phase 1 verification satisfies CONF-01, CONF-02, CONF-03, and OPS-03 with concrete evidence.
    - Phase 3 verification satisfies SRC-01, SRC-02, SRC-03, and RECON-01 with concrete evidence.
    - Phase 4 verification satisfies STATE-01, STATE-03, STATE-04, OPS-01, and OPS-02 with concrete evidence, including the new recovery fix.
    - ROADMAP and STATE show Phase 5 complete.
  </acceptance_criteria>
  <action>Align the operator docs and sample configs with normal Docker deployment realities, then write explicit phase verification reports grounded in existing code/tests and the new recovery coverage. Update milestone tracking files as part of phase closeout.</action>
  <verify>
    <automated>mise exec -- go test ./internal/runtime ./internal/providers/docker ./internal/config ./internal/state ./cmd/docker-dns-sync -count=1</automated>
  </verify>
</task>

</tasks>

<verification>
- `mise exec -- go test ./internal/runtime -run 'ReconcilePlanApply|OwnershipBoundary|PreserveManualRecords|RestartRecovery|StaleOwned' -count=1`
- `mise exec -- go test ./internal/runtime ./internal/providers/docker ./internal/config ./internal/state ./cmd/docker-dns-sync -count=1`
</verification>

<success_criteria>
- Restart recovery now converges when owned records are missing remotely.
- Docker deployment instructions no longer point normal container operators at a localhost AdGuard default.
- Missing verification artifacts for Phases 1, 3, and 4 exist and close the audit evidence gaps.
- Phase 5 is marked complete in planning state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-audit-gaps-conf-01-conf-02-conf-03-ops-03-src-01-src-02-/05-01-SUMMARY.md`
</output>
