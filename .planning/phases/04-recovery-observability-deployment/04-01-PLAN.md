---
phase: 04-recovery-observability-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/contracts/source.go
  - internal/providers/docker/source.go
  - internal/providers/docker/source_test.go
  - internal/runtime/app.go
  - internal/runtime/app_test.go
autonomous: true
requirements:
  - STATE-01
  - STATE-03
must_haves:
  truths:
    - The daemon performs startup reconciliation, then keeps running a steady-state loop that reacts to Docker change hints.
    - Docker event stream disconnects trigger reconnect plus a reconciliation pass before the daemon resumes normal event handling.
    - Restart/reboot correctness continues to come from the existing persisted-state-plus-source-snapshot reconcile model.
    - Live watch support remains optional so startup-only sources still satisfy the base source contract.
  artifacts:
    - path: internal/contracts/source.go
      provides: Optional live-watch contract adjacent to the existing required source interface.
    - path: internal/providers/docker/source.go
      provides: Docker event-watch implementation and reconnect-safe source hint stream.
    - path: internal/runtime/app.go
      provides: Startup-plus-steady-state runtime orchestration that reuses existing reconcile entrypoints.
    - path: internal/runtime/app_test.go
      provides: Runtime-loop coverage for startup, hint-triggered reconcile, and reconnect recovery.
  key_links:
    - from: internal/providers/docker/source.go
      to: internal/runtime/app.go
      via: Runtime consumes optional source watch hints without Docker-specific event policy leakage
      pattern: Watch|ListDesired|startupReconcile
    - from: internal/runtime/app.go
      to: internal/runtime/reconcile.go
      via: Steady-state loop reuses the same reconcile-and-persist entrypoint as startup
      pattern: ReconcileAndPersist
---

## Phase Goal

**As an** operator, **I want to** have the daemon keep itself converged after startup and recover cleanly from Docker watch disconnects, **so that** my managed DNS rewrites stay correct without manual restarts or cleanup.

<objective>
Add the first steady-state runtime loop for Docker-driven reconciliation while preserving the existing startup reconcile model as the single correctness path.

Purpose: Fulfill live runtime recovery behavior without replacing the ownership-safe reconcile core.
Output: Optional source-watch contract, Docker event watch implementation, reconnect-aware runtime loop, and focused runtime/provider tests.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
@.planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
@.planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
@PRD.md
@internal/contracts/source.go
@internal/providers/docker/source.go
@internal/providers/docker/source_test.go
@internal/runtime/app.go
@internal/runtime/app_test.go

<interfaces>
From `internal/contracts/source.go`:
`Source { Provider() ProviderRef; ListDesired(context.Context) ([]DesiredRecord, error) }` remains required.

From `internal/runtime/app.go`:
`Run` already performs startup setup and `startupReconcile`; extend this path instead of adding a second runtime entrypoint.

From Moby events docs:
Event streams end on error and the caller must reopen them after failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing runtime-loop and Docker-watch tests</name>
  <read_first>
    - internal/runtime/app.go
    - internal/runtime/app_test.go
    - internal/providers/docker/source.go
    - internal/providers/docker/source_test.go
    - internal/contracts/source.go
    - .planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
    - .planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
  </read_first>
  <files>internal/runtime/app_test.go, internal/providers/docker/source_test.go</files>
  <acceptance_criteria>
    - Runtime tests cover startup reconcile followed by steady-state reconcile after a source hint.
    - Runtime tests cover Docker watch disconnect/error causing reconnect and a repair reconcile before resume.
    - Docker provider tests cover event-stream bridging through a fake client seam.
    - The targeted command fails before implementation because no watch seam or loop exists yet.
  </acceptance_criteria>
  <action>Add failing tests for a narrow optional watch flow. In `internal/runtime/app_test.go`, use stubs that expose a hint channel and error channel so tests can assert: startup reconcile still happens first; a later hint triggers another reconcile; and a watch error triggers reconnect behavior before another reconcile. In `internal/providers/docker/source_test.go`, add a fake event client seam that emits container lifecycle events and disconnects. Keep assertions at the contract level: runtime should receive trigger-worthy hints, not Docker event internals.</action>
  <verify>
    <automated>sh -c 'if mise exec -- go test ./internal/runtime ./internal/providers/docker -run "Watch|Reconnect|RuntimeReconcile|StartupReconcile" -count=1; then exit 1; else exit 0; fi'</automated>
  </verify>
  <done>Focused failing tests exist for steady-state watch handling and reconnect recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement optional source watch support and reconnect-aware runtime orchestration</name>
  <read_first>
    - internal/contracts/source.go
    - internal/providers/docker/source.go
    - internal/providers/docker/source_test.go
    - internal/runtime/app.go
    - internal/runtime/app_test.go
    - .planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
    - .planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
  </read_first>
  <files>internal/contracts/source.go, internal/providers/docker/source.go, internal/runtime/app.go, internal/runtime/app_test.go</files>
  <behavior>
    - Test 1: Startup reconcile still runs before the runtime enters steady state.
    - Test 2: Docker watch hints trigger a new reconcile cycle using the existing snapshot path.
    - Test 3: Docker watch disconnects cause bounded reconnect attempts and a reconcile pass before resumed steady state.
    - Test 4: Sources without watch support still run correctly in startup-only mode.
  </behavior>
  <acceptance_criteria>
    - The base source contract remains snapshot-capable without mandatory watch implementation.
    - `internal/providers/docker/source.go` implements a narrow watch seam using the existing Docker provider.
    - `internal/runtime/app.go` adds a steady-state loop that reuses startup reconcile behavior instead of duplicating diff/apply logic.
    - Targeted runtime and Docker-provider tests pass without widening config or adding dependencies.
  </acceptance_criteria>
  <action>Introduce a small optional watch interface adjacent to `contracts.Source`, then implement it for the Docker provider using the Moby events stream with conservative event filtering. In runtime, factor startup and steady-state reconcile into a shared helper, start watch loops only for watch-capable sources, and on watch errors run the reconnect-and-repair flow before resuming. Keep event semantics coarse: a qualifying event should trigger a fresh snapshot-based reconcile, not direct record mutation. Reuse the existing retry config for reconnect pacing if needed, but do not change state ordering or ownership rules.</action>
  <verify>
    <automated>mise exec -- go test ./internal/runtime ./internal/providers/docker -run "Watch|Reconnect|RuntimeReconcile|StartupReconcile" -count=1</automated>
  </verify>
  <done>The daemon can stay converged after startup and recover safely from Docker watch disconnects through the existing reconcile model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker events -> source watch | Untrusted upstream event streams must not become direct mutation instructions. |
| source watch -> runtime loop | Hints may be dropped or disconnected, so runtime must recover via reconciliation rather than event trust alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `internal/providers/docker/source.go` | mitigate | Treat events as hints only and rerun snapshot derivation through existing source logic. |
| T-04-02 | Denial of Service | `internal/runtime/app.go` | mitigate | Bound reconnect pacing and require reconcile-based repair after disconnect before resuming steady state. |
| T-04-SC | Tampering | package installs | accept | No package-manager changes are part of this plan. |
</threat_model>

<verification>
- `mise exec -- go test ./internal/runtime -run 'Watch|Reconnect|RuntimeReconcile|StartupReconcile' -count=1`
- `mise exec -- go test ./internal/providers/docker -run 'Watch|Reconnect' -count=1`
- `mise exec -- go test ./internal/runtime ./internal/providers/docker -count=1`
</verification>

<success_criteria>
- Runtime no longer blocks forever immediately after startup reconcile when watch-capable sources are present.
- Docker watch errors lead to reconnect plus repair reconcile.
- Restart/reboot recovery still relies on the existing state-plus-snapshot reconcile path.
- Startup-only sources remain valid without implementing live watch support.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recovery-observability-deployment/04-01-SUMMARY.md`
</output>
