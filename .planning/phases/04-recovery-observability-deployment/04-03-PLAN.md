---
phase: 04-recovery-observability-deployment
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - README.md
  - Dockerfile
  - deploy/systemd/docker-dns-sync.service
  - testdata/config/example.toml
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - Operators have a documented host-binary path using `systemd` and a documented Docker path using the built container artifact.
    - Both deployment paths invoke the real CLI contract and preserve access to config, Docker, and state storage.
    - Deployment artifacts explain privileged Docker access clearly and do not imply unsafe hidden defaults.
    - In-repo examples stay consistent with the current config schema and runtime behavior.
  artifacts:
    - path: README.md
      provides: Operator setup, config expectations, and deployment instructions.
    - path: Dockerfile
      provides: Container build path for Docker deployment mode.
    - path: deploy/systemd/docker-dns-sync.service
      provides: Host-binary service unit for Linux operators.
    - path: testdata/config/example.toml
      provides: Example runtime configuration consumed by both deployment guides.
  key_links:
    - from: README.md
      to: cmd/docker-dns-sync/main.go
      via: Deployment instructions use the real `-config` CLI contract
      pattern: -config
    - from: deploy/systemd/docker-dns-sync.service
      to: cmd/docker-dns-sync/main.go
      via: Service unit executes the shipped binary with an explicit config path
      pattern: ExecStart
---

## Phase Goal

**As an** operator, **I want to** deploy the daemon either as a host service or a Docker container using repo-provided instructions, **so that** I can run it in my environment without reverse-engineering mounts, permissions, or restart settings.

<objective>
Ship the first operator-facing deployment artifacts and docs for both supported runtime modes.

Purpose: Complete Phase 4 by making the already-hardened daemon runnable in first-class documented host and Docker environments.
Output: README, example config, Dockerfile, and `systemd` unit.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
@.planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
@.planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
@.planning/phases/04-recovery-observability-deployment/04-01-SUMMARY.md
@.planning/phases/04-recovery-observability-deployment/04-02-SUMMARY.md
@cmd/docker-dns-sync/main.go
@internal/config/model.go
@internal/config/validate.go

<interfaces>
From `cmd/docker-dns-sync/main.go`:
The binary contract is `docker-dns-sync -config <path>`.

From `internal/config/model.go`:
Example config must populate `sources`, `outputs`, `state`, `logging`, and `retry` using the existing schema only.

From Docker/systemd docs:
Deployment artifacts should express restart behavior explicitly and document privileged Docker access requirements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing structural checks for deployment artifacts and docs</name>
  <read_first>
    - cmd/docker-dns-sync/main.go
    - internal/config/model.go
    - .planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
    - .planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
  </read_first>
  <files>README.md, Dockerfile, deploy/systemd/docker-dns-sync.service, testdata/config/example.toml</files>
  <acceptance_criteria>
    - Structural checks fail before implementation because the repo lacks the required docs/artifacts.
    - Planned docs explicitly reference config path, state path, and Docker access requirements.
  </acceptance_criteria>
  <action>Add the structural verification targets to the plan and confirm they currently fail because the repository does not yet contain a Dockerfile, service unit, or README-level operator docs. Capture the exact file set and CLI/config contract the final artifacts must reference so implementation stays tight.</action>
  <verify>
    <automated>sh -c 'test -f README.md && test -f Dockerfile && test -f deploy/systemd/docker-dns-sync.service && test -f testdata/config/example.toml && exit 1 || exit 0'</automated>
  </verify>
  <done>Wave-0 structural expectations are locked before deployment artifact implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement host-binary and Docker deployment artifacts plus operator docs</name>
  <read_first>
    - cmd/docker-dns-sync/main.go
    - internal/config/model.go
    - internal/config/validate.go
    - .planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
    - .planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
  </read_first>
  <files>README.md, Dockerfile, deploy/systemd/docker-dns-sync.service, testdata/config/example.toml</files>
  <behavior>
    - Test 1: Host-binary docs use the real CLI and document `systemd` restart behavior, config location, and writable state path.
    - Test 2: Docker docs use the real CLI and document config mount, persistent state mount, and Docker socket or proxy access.
    - Test 3: Example config validates against the current schema without adding new fields.
    - Test 4: Artifacts avoid embedding secrets and show env-ref usage where credentials are needed.
  </behavior>
  <acceptance_criteria>
    - `README.md` provides concise setup instructions for both deployment modes.
    - `Dockerfile` builds a runnable image for the existing binary entrypoint.
    - `deploy/systemd/docker-dns-sync.service` is a sensible starting service unit for Linux operators.
    - `testdata/config/example.toml` matches the current config model and uses explicit secret-reference patterns.
  </acceptance_criteria>
  <action>Create the minimal operator-facing deployment surface: a README with overview, config guidance, and separate host/Docker setup steps; a Dockerfile that builds the daemon binary and runs it with `-config`; a `systemd` unit that points to the binary and config path with sane restart behavior; and an example TOML config under `testdata/config`. Keep the docs concrete about required mounts, writable state path, and Docker socket privilege. Prefer small, real artifacts over speculative packaging extras.</action>
  <verify>
    <automated>test -f README.md && test -f Dockerfile && test -f deploy/systemd/docker-dns-sync.service && test -f testdata/config/example.toml</automated>
  </verify>
  <done>The repo contains first-class deployment docs and artifacts for both supported operator environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment -> deployment artifacts | Docs and sample artifacts must not hide privileged requirements or unsafe mounts. |
| example config -> runtime | Example configuration must demonstrate secret refs and writable state paths without embedding credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | deployment artifacts | mitigate | Document Docker socket privilege explicitly and provide least-privilege host/container defaults where feasible. |
| T-04-SC | Tampering | package installs | accept | No package-manager changes are part of this plan. |
</threat_model>

<verification>
- `test -f README.md && test -f Dockerfile && test -f deploy/systemd/docker-dns-sync.service && test -f testdata/config/example.toml`
- `grep -n '\-config' README.md deploy/systemd/docker-dns-sync.service Dockerfile`
</verification>

<success_criteria>
- The repo documents both host-binary and Docker deployment paths.
- Deployment artifacts invoke the existing binary contract and match the current config schema.
- Example config avoids embedded secrets and shows operator-safe patterns.
- Operators have enough information to persist state and access Docker intentionally.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recovery-observability-deployment/04-03-SUMMARY.md`
</output>
