---
phase: 04-recovery-observability-deployment
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
files_modified:
  - internal/runtime/app.go
  - internal/runtime/deps.go
  - internal/providers/adguard/output.go
  - internal/providers/adguard/output_test.go
  - internal/runtime/app_test.go
autonomous: true
requirements:
  - STATE-04
  - OPS-01
must_haves:
  truths:
    - Temporary AdGuard Home failures are retried with the configured runtime backoff until the operation succeeds or the retry budget is exhausted.
    - Retry behavior never advances ownership state before successful remote visibility/apply operations complete.
    - Structured logs expose reconcile lifecycle, retry attempts, output mutations, and state persistence with provider identity and without secrets.
    - Runtime recovery behavior remains centralized and compatible with the existing reconcile core.
  artifacts:
    - path: internal/runtime/app.go
      provides: Retry-aware reconcile orchestration and structured lifecycle logging.
    - path: internal/runtime/deps.go
      provides: Runtime retry helpers or parsed values reused by recovery logic.
    - path: internal/providers/adguard/output_test.go
      provides: Failure/retry coverage for AdGuard transport behavior or wrappers.
  key_links:
    - from: internal/runtime/app.go
      to: internal/runtime/reconcile.go
      via: Retry wrapper preserves reconcile-and-persist ordering
      pattern: ReconcileAndPersist
    - from: internal/runtime/app.go
      to: internal/providers/adguard/output.go
      via: Runtime logs and retries output-facing read/write calls without changing provider ownership semantics
      pattern: ListVisible|Create|Update|Delete
---

## Phase Goal

**As an** operator, **I want to** have temporary AdGuard Home outages heal automatically and be visible in logs, **so that** the daemon converges again without manual intervention and I can understand what it is doing.

<objective>
Add retry-aware output/runtime behavior and structured operator logs around reconcile, recovery, and persistence.

Purpose: Complete the runtime hardening side of Phase 4 after live watch/reconnect behavior exists.
Output: Bounded backoff around temporary output failures, operator-usable structured logs, and focused tests.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
@.planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
@.planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
@.planning/phases/04-recovery-observability-deployment/04-01-SUMMARY.md
@internal/runtime/app.go
@internal/runtime/deps.go
@internal/runtime/reconcile.go
@internal/providers/adguard/output.go
@internal/providers/adguard/output_test.go

<interfaces>
From `internal/runtime/deps.go`:
`RetryPolicy { InitialInterval, MaxInterval, MaxElapsedTime }` is already parsed from config.

From `internal/providers/adguard/output.go`:
AdGuard output already provides `ListVisible`, `Create`, `Update`, and `Delete` with explicit request errors.

From `internal/runtime/reconcile.go`:
`ReconcileAndPersist` must remain the only place state is saved after successful apply.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing outage-retry and structured-log tests</name>
  <read_first>
    - internal/runtime/app.go
    - internal/runtime/deps.go
    - internal/providers/adguard/output.go
    - internal/providers/adguard/output_test.go
    - internal/runtime/app_test.go
    - .planning/phases/04-recovery-observability-deployment/04-CONTEXT.md
    - .planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
  </read_first>
  <files>internal/runtime/app_test.go, internal/providers/adguard/output_test.go</files>
  <acceptance_criteria>
    - Tests cover transient visible-state or apply failures that later succeed and confirm retry before success.
    - Tests confirm state is not persisted ahead of successful output operations.
    - Tests confirm structured logs include retry attempt context and recovery lifecycle markers.
    - The targeted command fails before implementation because no retry-aware runtime behavior or log assertions exist yet.
  </acceptance_criteria>
  <action>Add failing tests around a transiently failing AdGuard output stub or transport seam. Cover at least one read-side failure (`ListVisible`) and one write-side failure (`Create`/`Update`/`Delete`) that succeeds after retry. Capture logger output in tests and assert the presence of fields such as operation, provider, attempt, and error without asserting secret values. Keep tests narrow and deterministic by avoiding sleeps larger than tiny retry intervals under test config.</action>
  <verify>
    <automated>sh -c 'if mise exec -- go test ./internal/runtime ./internal/providers/adguard -run "Retry|Outage|Logging" -count=1; then exit 1; else exit 0; fi'</automated>
  </verify>
  <done>Failing tests exist for AdGuard outage retries and structured runtime logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded retry behavior and structured recovery logs</name>
  <read_first>
    - internal/runtime/app.go
    - internal/runtime/deps.go
    - internal/runtime/reconcile.go
    - internal/providers/adguard/output.go
    - internal/providers/adguard/output_test.go
    - internal/runtime/app_test.go
    - .planning/phases/04-recovery-observability-deployment/04-RESEARCH.md
    - .planning/phases/04-recovery-observability-deployment/04-PATTERNS.md
  </read_first>
  <files>internal/runtime/app.go, internal/runtime/deps.go, internal/providers/adguard/output.go, internal/runtime/app_test.go</files>
  <behavior>
    - Test 1: Transient AdGuard failures retry and eventually converge.
    - Test 2: Terminal failures return errors without persisting speculative state.
    - Test 3: Logs expose startup reconcile, steady-state reconcile, retries, output writes, and state persistence boundaries.
    - Test 4: Logs do not expose credentials or resolved secret values.
  </behavior>
  <acceptance_criteria>
    - Retry behavior uses the existing configured retry policy with bounded intervals/elapsed time.
    - Retry and log orchestration stays in runtime or a narrow wrapper and does not move ownership policy into providers.
    - Targeted runtime and AdGuard tests pass without adding dependencies.
  </acceptance_criteria>
  <action>Implement a small retry helper driven by `RuntimeDeps.Retry` and use it around the output-facing operations that can fail transiently during reconciliation or recovery. Add structured logs around reconcile start/finish, retry attempts, remote writes, and state persistence so operators can trace runtime activity. Keep provider error strings sanitized and preserve the existing invariant that state only saves after successful apply. Avoid broad refactors; prefer a small wrapper/helper over a new subsystem.</action>
  <verify>
    <automated>mise exec -- go test ./internal/runtime ./internal/providers/adguard -run "Retry|Outage|Logging" -count=1</automated>
  </verify>
  <done>Temporary AdGuard outages are retried safely and operators can trace recovery behavior from structured logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime retry loop -> output provider | Recovery logic must not convert transient transport failures into incorrect ownership state. |
| logger -> operator output | Logs must provide enough context to debug failures without exposing credentials or secret refs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | `internal/runtime/app.go` | mitigate | Bound retries with configured elapsed time and log attempts for operator visibility. |
| T-04-04 | Information Disclosure | runtime logging | mitigate | Emit structured operational context only; never log credentials, passwords, or resolved secret values. |
| T-04-SC | Tampering | package installs | accept | No package-manager changes are part of this plan. |
</threat_model>

<verification>
- `mise exec -- go test ./internal/runtime -run 'Retry|Outage|Logging' -count=1`
- `mise exec -- go test ./internal/providers/adguard -run 'Retry|Logging' -count=1`
- `mise exec -- go test ./internal/runtime ./internal/providers/adguard -count=1`
</verification>

<success_criteria>
- Temporary AdGuard failures no longer require manual daemon restart to converge.
- Retry behavior is bounded and visible in structured logs.
- State persistence still happens only after successful remote apply.
- Logs cover runtime recovery without disclosing secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-recovery-observability-deployment/04-02-SUMMARY.md`
</output>
