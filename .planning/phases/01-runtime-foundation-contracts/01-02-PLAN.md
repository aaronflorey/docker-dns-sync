---
phase: 01-runtime-foundation-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/config/model.go
  - internal/config/validate.go
  - internal/config/secrets.go
  - internal/config/validate_test.go
  - testdata/config/socket-proxy.toml
  - testdata/config/env-secret.toml
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
user_setup: []
must_haves:
  truths:
    - Operator gets a clear startup error when sources, outputs, or required runtime fields are missing.
    - Operator can configure Docker endpoint mode, state path, log level, retry settings, and credential references through TOML alone.
    - Secret references resolve from environment-backed values without printing resolved secrets.
  artifacts:
    - path: internal/config/validate.go
      provides: Semantic config validation
      contains: func Validate
    - path: internal/config/secrets.go
      provides: Secret reference resolution
      contains: func ResolveSecrets
    - path: internal/config/validate_test.go
      provides: Requirement-level config tests
      contains: TestDockerEndpointModes
  key_links:
    - from: internal/config/validate.go
      to: internal/config/model.go
      via: semantic validation over decoded config
      pattern: Config
    - from: internal/config/secrets.go
      to: testdata/config/env-secret.toml
      via: env-ref fixture coverage
      pattern: ENV:
---

## Phase Goal

**As an** operator, **I want to** configure and start the daemon with stable source/output extension points, **so that** I can change runtime wiring from TOML and future providers can plug in without redesign.

<objective>
Turn the thin bootstrap into a safe config surface that rejects unusable input and resolves secret references correctly.

Purpose: Lock the Phase 1 config invariants so later provider and state slices build on one deterministic startup contract.
Output: Semantic validation, secret-resolution helpers, endpoint-mode fixtures, and focused config tests.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-foundation-contracts/01-RESEARCH.md
@PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config-validation test coverage and supporting fixtures</name>
  <files>internal/config/validate_test.go, testdata/config/socket-proxy.toml, testdata/config/env-secret.toml</files>
  <read_first>internal/config/model.go, internal/config/load.go, .planning/REQUIREMENTS.md, .planning/phases/01-runtime-foundation-contracts/01-RESEARCH.md</read_first>
  <action>Create config tests and fixtures for the locked Phase 1 behaviors: require 1+ sources and 1+ outputs per D-02, accept Docker endpoints for both `unix:///var/run/docker.sock` and proxy/TCP forms per D-03, validate the already-established `[state]`, `[logging]`, and `[retry]` sections from `minimal.toml`, and require exactly one credential source between inline value and env reference. Use `socket-proxy.toml` and `env-secret.toml` fixtures to keep the tests concrete. Make the secret tests assert on redacted error text rather than raw env values.</action>
  <verify><automated>test -f internal/config/validate_test.go && test -f testdata/config/socket-proxy.toml && test -f testdata/config/env-secret.toml</automated></verify>
  <acceptance_criteria>
    - internal/config/validate_test.go contains `TestValidateRequiresSourceAndOutput`
    - internal/config/validate_test.go contains `TestDockerEndpointModes`
    - testdata/config/socket-proxy.toml contains `endpoint = "tcp://`
    - testdata/config/env-secret.toml contains `ENV:`
  </acceptance_criteria>
  <done>The config package has failing tests that describe CONF-01, CONF-02, and CONF-03 in executable form.</done>
</task>

<task type="auto">
  <name>Task 2: Implement validation and secret resolution without leaking credentials</name>
  <files>internal/config/model.go, internal/config/validate.go, internal/config/secrets.go</files>
  <read_first>internal/config/validate_test.go, .planning/research/PITFALLS.md, .planning/research/STACK.md</read_first>
  <action>Implement semantic validation and secret resolution per D-01, D-02, and D-03. Keep the root config explicit and boring: validate `sources`, `outputs`, `state.path`, `logging.level`, and retry fields after TOML decode; accept Docker `unix://` and proxy-style endpoints without requiring the local socket; and implement env-backed secret references with exact-one-of semantics for credential inputs. Do not log or embed resolved secret values in returned errors. Do not add a separate CLI command in this slice; keep startup validation on the existing run path.</action>
  <verify><automated>mise exec -- go test ./internal/config -run 'TestValidateRequiresSourceAndOutput|TestValidateRuntimeAndCredentialFields|TestDockerEndpointModes|TestResolveSecrets' -count=1</automated></verify>
  <acceptance_criteria>
    - internal/config/validate.go contains `func Validate`
    - internal/config/secrets.go contains `func ResolveSecrets`
    - internal/config/secrets.go does not contain `slog.`
  </acceptance_criteria>
  <done>Config validation and secret resolution satisfy the Phase 1 requirement tests and keep secret material out of logs and error strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator TOML → semantic validator | Parsed config values can request unsafe defaults or ambiguous credential sources. |
| process environment → secret resolver | Environment values are sensitive and must not leak into logs or errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | internal/config/secrets.go | mitigate | Resolve env refs without logging resolved values; keep test assertions on redacted messages only. |
| T-01-05 | Tampering | internal/config/validate.go | mitigate | Enforce required sections and exact-one credential-source rules so malformed config cannot silently degrade behavior. |
| T-01-06 | Elevation of Privilege | Docker endpoint config | accept | Endpoint selection is operator-controlled by design in Phase 1; validation only constrains syntax, not network reachability. |
| T-01-SC | Tampering | Go dependency install | mitigate | Do not add optional `backoff/v5` in this slice because research marks it optional `[ASSUMED]`; stay within already-audited stdlib + BurntSushi/toml. |
</threat_model>

<verification>
- [ ] `mise exec -- go test ./internal/config -run 'TestValidateRequiresSourceAndOutput|TestValidateRuntimeAndCredentialFields|TestDockerEndpointModes|TestResolveSecrets' -count=1`
- [ ] `mise exec -- go test ./...`
- [ ] `mise exec -- go run ./cmd/docker-dns-sync -config testdata/config/env-secret.toml` succeeds when required env vars are exported and does not print secret values
</verification>

<success_criteria>
- Startup semantics enforce the Phase 1 config contract instead of relying on hidden code defaults.
- Docker local-socket and proxy endpoint forms are configuration-driven per D-03.
- Secret references are resolved safely and are never written to logs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation-contracts/01-02-SUMMARY.md`
</output>
