---
phase: 01-runtime-foundation-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - mise.toml
  - cmd/docker-dns-sync/main.go
  - cmd/docker-dns-sync/main_test.go
  - internal/config/model.go
  - internal/config/load.go
  - internal/runtime/app.go
  - testdata/config/minimal.toml
  - testdata/config/malformed.toml
autonomous: true
requirements:
  - CONF-01
user_setup: []
must_haves:
  truths:
    - Operator can start the daemon with a real TOML file containing at least one source block and one output block.
    - Missing config path, unreadable files, and malformed TOML fail before the runtime loop starts.
    - The repo has a reproducible daemon entrypoint that enters a minimal long-running lifecycle and can be cancelled cleanly.
  artifacts:
    - path: go.mod
      provides: Go module pinned to Go 1.26 toolchain
      contains: toolchain go1.26.3
    - path: cmd/docker-dns-sync/main.go
      provides: CLI entrypoint with -config startup path
      contains: flag.String("config"
    - path: internal/config/model.go
      provides: Root runtime config model
      contains: type Config struct
    - path: testdata/config/minimal.toml
      provides: Minimal walking-skeleton config fixture with the full Phase 1 startup contract
      contains: "[[sources]]"
    - path: testdata/config/malformed.toml
      provides: Malformed TOML startup fixture
      contains: "[sources"
  key_links:
    - from: cmd/docker-dns-sync/main.go
      to: internal/config/load.go
      via: config file load before runtime start
      pattern: config\.Load
    - from: cmd/docker-dns-sync/main.go
      to: internal/runtime/app.go
      via: startup handoff
      pattern: runtime\.New
    - from: cmd/docker-dns-sync/main.go
      to: internal/runtime/app.go
      via: daemon lifecycle start and cancellation handling
      pattern: App\.Run
---

## Phase Goal

**As an** operator, **I want to** configure and start the daemon with stable source/output extension points, **so that** I can change runtime wiring from TOML and future providers can plug in without redesign.

<objective>
Bootstrap the repository into a runnable Go daemon skeleton that can load TOML and enter a minimal runtime path.

Purpose: Establish the first end-to-end operator path for Phase 1 without pulling in provider behavior or reconciliation.
Output: Go module/toolchain files, CLI entrypoint, root config model, minimal loader, lifecycle smoke tests, and sample TOML fixtures.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-foundation-contracts/01-RESEARCH.md
@PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create startup test scaffolding and Phase 1 config fixtures</name>
  <files>cmd/docker-dns-sync/main_test.go, testdata/config/minimal.toml, testdata/config/malformed.toml</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-runtime-foundation-contracts/01-CONTEXT.md, .planning/phases/01-runtime-foundation-contracts/01-RESEARCH.md, PRD.md</read_first>
  <action>Create the command-package test skeleton and the two config fixtures that define the Phase 1 startup contract. `testdata/config/minimal.toml` must already include explicit `[[sources]]`, `[[outputs]]`, `[state]`, `[logging]`, and `[retry]` sections so later plans do not change the meaning of a valid startup config. `testdata/config/malformed.toml` should contain intentionally broken TOML. Add named test cases for the happy path, missing-config error, malformed-config error, and cancelable run-loop behavior, but this task only needs to establish their structure and fixture references; Task 2 must supply the real assertions and make the suite pass before any Go verification is accepted.</action>
  <verify><automated>test -f cmd/docker-dns-sync/main_test.go && test -f testdata/config/minimal.toml && test -f testdata/config/malformed.toml</automated></verify>
  <acceptance_criteria>
    - cmd/docker-dns-sync/main_test.go contains `TestRunWithMinimalConfig`
    - cmd/docker-dns-sync/main_test.go contains `TestRunRejectsMissingConfig`
    - cmd/docker-dns-sync/main_test.go contains `TestRunRejectsMalformedConfig`
    - cmd/docker-dns-sync/main_test.go contains `TestRunBlocksUntilCancelled`
    - testdata/config/minimal.toml contains `[[sources]]` and `[[outputs]]`
    - testdata/config/minimal.toml contains `[state]`
    - testdata/config/minimal.toml contains `[logging]`
    - testdata/config/minimal.toml contains `[retry]`
    - testdata/config/malformed.toml contains `[sources`
  </acceptance_criteria>
  <done>The repo has concrete startup test scaffolding and fixtures that lock the full Phase 1 config contract before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the minimal config-driven CLI and cancellable runtime loop</name>
  <files>go.mod, mise.toml, cmd/docker-dns-sync/main.go, internal/config/model.go, internal/config/load.go, internal/runtime/app.go</files>
  <read_first>cmd/docker-dns-sync/main_test.go, testdata/config/minimal.toml, .planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Initialize the Go module and `mise` toolchain pin for Go 1.26.3, add `github.com/BurntSushi/toml`, and implement the thinnest real daemon run path. Per D-01 and D-04, define one root `Config` struct with nested `sources`, `outputs`, `state`, `logging`, and `retry` sections in `internal/config/model.go`; load TOML from `-config` in `internal/config/load.go`; and have `cmd/docker-dns-sync/main.go` call `runtime.New(cfg).Run(ctx)` in `internal/runtime/app.go`. Before running the Go verification command, replace the Task 1 scaffolding with real assertions so `TestRunWithMinimalConfig`, `TestRunRejectsMissingConfig`, `TestRunRejectsMalformedConfig`, and `TestRunBlocksUntilCancelled` prove actual startup behavior rather than file existence. Keep `Run` intentionally thin but long-lived: log startup metadata, block on context cancellation or signal-driven shutdown, and avoid provider bootstrapping, Docker discovery, reconciliation, or state persistence in this plan.</action>
  <verify><automated>mise exec -- go test ./cmd/docker-dns-sync ./internal/config ./internal/runtime -run 'TestRunWithMinimalConfig|TestRunRejectsMissingConfig|TestRunRejectsMalformedConfig|TestRunBlocksUntilCancelled' -count=1</automated></verify>
  <acceptance_criteria>
    - go.mod contains `toolchain go1.26.3`
    - cmd/docker-dns-sync/main.go contains `flag.String("config"`
    - internal/config/model.go contains `type Config struct`
    - internal/runtime/app.go contains `func (a *App) Run`
    - internal/runtime/app.go contains `<-ctx.Done()`
    - cmd/docker-dns-sync/main_test.go does not contain `t.Skip(`
  </acceptance_criteria>
  <done>Running the daemon with `-config testdata/config/minimal.toml` exercises a real TOML-driven daemon startup path that stays alive until cancelled, and the initial smoke tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator TOML → config loader | Untrusted local configuration enters the process and controls runtime settings. |
| CLI args → runtime bootstrap | Invalid file paths and flags can trigger unsafe startup behavior if not validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | internal/config/load.go | mitigate | Fail fast on missing/unreadable/malformed config before runtime start; no implicit defaults that bypass required sections. |
| T-01-02 | Denial of Service | cmd/docker-dns-sync/main.go | mitigate | Reject missing config path and startup errors immediately; only enter the run loop after successful bootstrap. |
| T-01-03 | Repudiation | internal/runtime/app.go | accept | Minimal startup logs are enough in this slice because no mutating behavior exists yet. |
| T-01-SC | Tampering | Go dependency install | mitigate | Only add `github.com/BurntSushi/toml` from the audited Phase 1 research table; do not introduce optional `[ASSUMED]` packages in this plan. |
</threat_model>

<verification>
- [ ] `mise exec -- go test ./cmd/docker-dns-sync ./internal/config ./internal/runtime -run 'TestRunWithMinimalConfig|TestRunRejectsMissingConfig|TestRunRejectsMalformedConfig|TestRunBlocksUntilCancelled' -count=1`
- [ ] `mise exec -- go test ./...`
- [ ] `timeout 5s mise exec -- go run ./cmd/docker-dns-sync -config testdata/config/minimal.toml` stays running until terminated and exits without bootstrap errors
</verification>

<success_criteria>
- The project bootstraps with a pinned Go toolchain and a runnable daemon entrypoint.
- The startup path reads a TOML file with explicit source/output blocks per D-01 and D-02.
- Error cases fail before runtime execution rather than panicking later.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation-contracts/01-01-SUMMARY.md`
</output>
