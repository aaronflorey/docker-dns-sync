---
phase: 03-docker-godoxy-snapshot-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/providers/docker/labels.go
  - internal/providers/docker/labels_test.go
  - internal/providers/docker/source_test.go
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
must_haves:
  truths:
    - The repo has deterministic tests that freeze the exact MVP Godoxy subset for DNS rewrite derivation.
    - Containers marked with supported exclusion semantics do not produce desired rewrites.
    - Alias fallback, `proxy.aliases`, `proxy.#N.port`, and `proxy.*.port` forms map to the expected hostnames and answers.
    - Desired-record derivation remains local to the Docker provider and does not widen runtime or config contracts.
  artifacts:
    - path: internal/providers/docker/labels.go
      provides: Narrow Godoxy-subset label expansion and desired-record derivation helpers.
    - path: internal/providers/docker/labels_test.go
      provides: Compatibility coverage for exclusion, alias fallback, wildcard/reference ports, and host-target precedence.
    - path: internal/providers/docker/source_test.go
      provides: Container-fixture-to-desired-record coverage that exercises the subset helpers through the provider seam.
  key_links:
    - from: internal/providers/docker/labels.go
      to: internal/contracts/source.go
      via: Desired-record normalization and shared source lineage shape
      pattern: DesiredRecord|SourceObjectRef
    - from: internal/providers/docker/source_test.go
      to: internal/providers/docker/source.go
      via: Provider ListDesired seam exercised through fake Docker summaries
      pattern: ListDesired
---

## Phase Goal

**As an** operator, **I want to** have current Godoxy-labeled Docker workloads turned into the right desired DNS rewrites on startup, **so that** the daemon can reconcile from real source state without manual record calculation.

<objective>
Lock the exact Phase 03 Godoxy compatibility subset in tests before wiring the full Docker snapshot provider.

Purpose: Freeze D-05 through D-09 into deterministic parsing/derivation behavior so the later provider implementation only has to list containers and call the locked helpers.
Output: Narrow Docker label helpers plus failing-then-passing compatibility tests for exclusion, alias fallback, supported port forms, and answer-target precedence.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-CONTEXT.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-RESEARCH.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
@PRD.md
@internal/contracts/source.go
@internal/providers/docker/source.go
@internal/runtime/app.go

<interfaces>
From internal/contracts/source.go:
`ProviderRef { Type, Name }`, `SourceObjectRef { Provider, ID, DisplayName }`, `DesiredRecord { Hostname, Answer, Source }`, `Source.ListDesired(context.Context) ([]DesiredRecord, error)`.

From internal/providers/docker/source.go:
`Provider { ref, endpoint, client }` already exists and currently stubs `ListDesired`.

From PRD.md / 03-CONTEXT.md:
Supported Phase 03 subset is `proxy.aliases`, `proxy.exclude`, fallback-to-container-name, `proxy.<alias>.port`, `proxy.#N.port`, `proxy.*.port`, and supported host override precedence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing subset-compatibility tests and provider fixture coverage</name>
  <read_first>
    - internal/providers/docker/source.go
    - internal/contracts/source.go
    - internal/runtime/app_test.go
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-CONTEXT.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-RESEARCH.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
  </read_first>
  <files>internal/providers/docker/labels_test.go, internal/providers/docker/source_test.go</files>
  <acceptance_criteria>
    - `internal/providers/docker/labels_test.go` covers alias fallback, `proxy.aliases`, exclusion, `proxy.#N.port`, `proxy.*.port`, and explicit host override behavior.
    - `internal/providers/docker/source_test.go` defines table-driven container-fixture cases that expect exact `contracts.DesiredRecord` outputs or skips.
    - The targeted Docker-provider test command fails before implementation because the helpers and behaviors do not yet exist.
  </acceptance_criteria>
  <action>Create failing table-driven tests that encode the frozen Godoxy subset from D-05 through D-09. Use Docker summary fixtures with container ID, names, labels, and minimal network metadata to assert exact desired hostname/answer/source output. Keep coverage intentionally narrow: no middleware, homepage, YAML blob, or watch behavior. Name the top-level tests `TestDockerLabelsSubset`, `TestDesiredRecordDerivation`, and `TestExcludedContainersProduceNoDesiredRecords` so later verification commands stay stable.</action>
  <verify>
    <automated>sh -c 'if mise exec -- go test ./internal/providers/docker -run "DockerLabelsSubset|DesiredRecordDerivation|ExcludedContainersProduceNoDesiredRecords" -count=1; then exit 1; else exit 0; fi'</automated>
  </verify>
  <done>Focused failing tests exist that freeze the supported Godoxy subset before Docker snapshot implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement narrow Godoxy-subset derivation helpers</name>
  <read_first>
    - internal/providers/docker/labels.go (create in this task)
    - internal/providers/docker/labels_test.go
    - internal/providers/docker/source_test.go
    - internal/contracts/source.go
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-CONTEXT.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-RESEARCH.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
  </read_first>
  <files>internal/providers/docker/labels.go</files>
  <behavior>
    - Test 1: Containers without aliases fall back to a normalized container-name hostname.
    - Test 2: `proxy.exclude=true` suppresses all desired records for the container.
    - Test 3: `proxy.aliases`, `proxy.#N.port`, and `proxy.*.port` derive the expected alias hostnames while staying within the supported subset.
    - Test 4: Explicit supported host override is used as the answer before the derived default host target.
  </behavior>
  <acceptance_criteria>
    - `internal/providers/docker/labels.go` contains small pure helpers that derive desired records from container metadata without depending on runtime or state packages beyond `internal/contracts`.
    - Final desired-record output is deterministic and sorted for stable tests and reconcile behavior.
    - The targeted Docker-provider tests pass without adding new dependencies or widening config.
  </acceptance_criteria>
  <action>Implement a narrow helper layer inside `internal/providers/docker` that mirrors only the supported Godoxy subset. Follow `03-PATTERNS.md` by keeping the logic pure and deterministic: parse aliases, detect exclusion, expand supported `#N` and `*` port forms only as needed to identify alias participation, apply host-target precedence, and return sorted `contracts.DesiredRecord` slices with shared `SourceObjectRef` lineage per container. Do not import or replicate Godoxy's full parser stack; optimize for the exact Phase 03 subset and tests.</action>
  <verify>
    <automated>mise exec -- go test ./internal/providers/docker -run "DockerLabelsSubset|DesiredRecordDerivation|ExcludedContainersProduceNoDesiredRecords" -count=1</automated>
  </verify>
  <done>The Docker provider package contains passing compatibility helpers that freeze the supported Phase 03 label subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker labels → parser helpers | Untrusted container metadata must not generate unsupported or ambiguous desired rewrites silently. |
| parser helpers → source output | Derived hostnames/answers become the desired-state authority for startup reconcile and must remain deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | internal/providers/docker/labels.go | mitigate | Freeze supported label semantics in tests and ignore unsupported broader Godoxy fields. |
| T-03-02 | Denial of Service | internal/providers/docker/labels.go | mitigate | Keep parsing narrow and deterministic; skip excluded/unsupported cases instead of attempting broad best-effort behavior. |
| T-03-SC | Tampering | package installs | accept | No package-manager changes are part of this plan. |
</threat_model>

<verification>
- `mise exec -- go test ./internal/providers/docker -run "DockerLabelsSubset|DesiredRecordDerivation|ExcludedContainersProduceNoDesiredRecords" -count=1`
- `mise exec -- go test ./internal/providers/docker -count=1`
</verification>

<success_criteria>
- The exact MVP Godoxy subset is encoded in provider-level tests before snapshot integration.
- Excluded containers emit no desired records.
- Alias fallback and supported alias label patterns produce deterministic desired hostname/answer pairs.
- No new config or broader Godoxy parser surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-docker-godoxy-snapshot-automation/03-01-SUMMARY.md`
</output>
