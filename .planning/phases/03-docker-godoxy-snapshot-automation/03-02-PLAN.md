---
phase: 03-docker-godoxy-snapshot-automation
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01-PLAN.md
files_modified:
  - internal/providers/docker/source.go
  - internal/providers/docker/source_test.go
  - internal/runtime/app_test.go
  - internal/runtime/factories_test.go
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - RECON-01
must_haves:
  truths:
    - The Docker provider can list current containers and convert eligible labeled containers into `contracts.DesiredRecord` snapshots.
    - Startup reconciliation consumes real Docker source output without any new startup path or runtime contract changes.
    - Container lineage stays stable via Docker container ID and display name in `contracts.SourceObjectRef`.
    - Snapshot output is deterministic and excludes unsupported or explicitly excluded containers conservatively.
  artifacts:
    - path: internal/providers/docker/source.go
      provides: Real Docker snapshot `ListDesired` implementation over the current Moby client seam.
    - path: internal/providers/docker/source_test.go
      provides: Fake-client coverage for container listing, filtering, and desired-record output.
    - path: internal/runtime/app_test.go
      provides: Startup reconcile coverage proving real source output still feeds the existing runtime path.
  key_links:
    - from: internal/providers/docker/source.go
      to: internal/runtime/app.go
      via: Runtime startupReconcile consumes Source.ListDesired output unchanged
      pattern: ListDesired|startupReconcile
    - from: internal/providers/docker/source.go
      to: internal/contracts/source.go
      via: Provider emits normalized DesiredRecord values with stable source lineage
      pattern: DesiredRecord|SourceObjectRef
---

## Phase Goal

**As an** operator, **I want to** start the daemon and have it reconcile against the current Docker/Godoxy source state immediately, **so that** my initial DNS rewrite set is correct before any future live-event handling is added.

<objective>
Implement the real Docker snapshot provider that plugs the already-tested Godoxy subset into the existing runtime startup reconcile flow.

Purpose: Complete the MVP source side for startup synchronization by filling the `ListDesired` stub in `internal/providers/docker/source.go` and proving runtime integration stays unchanged.
Output: Real Docker source snapshot listing, fake-client tests, and narrow startup/runtime assertions.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-CONTEXT.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-RESEARCH.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
@.planning/phases/03-docker-godoxy-snapshot-automation/03-01-SUMMARY.md
@internal/providers/docker/source.go
@internal/contracts/source.go
@internal/runtime/app.go
@internal/runtime/app_test.go
@internal/runtime/factories_test.go

<interfaces>
From internal/providers/docker/source.go:
`Provider.New` already builds a Moby client using `WithHost` and `WithAPIVersionNegotiation`; extend the internal client seam only as needed for snapshot listing.

From internal/contracts/source.go:
`ListDesired(context.Context) ([]DesiredRecord, error)` is the only source contract output surface.

From internal/runtime/app.go:
Startup already loads owned state, aggregates `ListDesired` across sources, lists visible output records, and calls `ReconcileAndPersist` before blocking on steady state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing Docker snapshot and startup integration tests</name>
  <read_first>
    - internal/providers/docker/source.go
    - internal/providers/docker/source_test.go
    - internal/runtime/app_test.go
    - internal/runtime/factories_test.go
    - internal/contracts/source.go
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-CONTEXT.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
  </read_first>
  <files>internal/providers/docker/source_test.go, internal/runtime/app_test.go, internal/runtime/factories_test.go</files>
  <acceptance_criteria>
    - `internal/providers/docker/source_test.go` covers container listing, running/eligible filtering, excluded-container skipping, and deterministic desired-record output through a fake Docker client.
    - Runtime tests continue to prove startup reconcile runs before steady state and can consume non-empty Docker source output without changing runtime contracts.
    - The targeted Docker-provider/runtime test command fails before implementation because `ListDesired` still returns `nil, nil` or client seams are incomplete.
  </acceptance_criteria>
  <action>Add failing tests around `Provider.ListDesired` using a fake Docker client that returns container summaries. Cover eligible running containers, explicitly excluded containers, missing-supported-label cases, and deterministic ordering across multiple containers. Extend existing runtime/factory tests only where needed to keep startup integration and endpoint wiring proven after the Docker client interface widens for container listing.</action>
  <verify>
    <automated>sh -c 'if mise exec -- go test ./internal/providers/docker ./internal/runtime -run "ListDesired|StartupReconcile|DockerSourceUsesConfiguredEndpoint" -count=1; then exit 1; else exit 0; fi'</automated>
  </verify>
  <done>Failing tests exist for real Docker snapshot listing and startup integration against the existing runtime path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Docker snapshot `ListDesired` and preserve runtime startup integration</name>
  <read_first>
    - internal/providers/docker/source.go
    - internal/providers/docker/source_test.go
    - internal/providers/docker/labels.go
    - internal/runtime/app.go
    - internal/runtime/app_test.go
    - internal/runtime/factories_test.go
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-RESEARCH.md
    - .planning/phases/03-docker-godoxy-snapshot-automation/03-PATTERNS.md
  </read_first>
  <files>internal/providers/docker/source.go, internal/runtime/app_test.go, internal/runtime/factories_test.go</files>
  <behavior>
    - Test 1: `ListDesired` lists current Docker containers and emits desired records only for eligible supported cases.
    - Test 2: Excluded or unsupported containers emit no desired records.
    - Test 3: Returned desired records preserve provider identity, container ID, display name, and deterministic ordering.
    - Test 4: Existing startup reconcile behavior remains unchanged and still runs before cancellation/steady-state blocking.
  </behavior>
  <acceptance_criteria>
    - `internal/providers/docker/source.go` implements `ListDesired` using the existing Docker provider with the smallest necessary client-interface expansion.
    - Provider code keeps Docker metadata translation local and does not import state/reconcile policy or add watch behavior.
    - Targeted Docker-provider and runtime tests pass without widening config or adding dependencies.
  </acceptance_criteria>
  <action>Implement `Provider.ListDesired` in `internal/providers/docker/source.go` by listing current containers through the Moby client, filtering to the supported Phase 03 eligibility cases, and converting each container through the locked helper behavior from Plan 03-01. Keep the client seam narrow, preserve `contracts.ProviderRef` and `SourceObjectRef` lineage, sort final desired records deterministically, and rely on the existing runtime startup path in `internal/runtime/app.go` unchanged. Update runtime/factory tests only where the widened source client seam requires it.</action>
  <verify>
    <automated>mise exec -- go test ./internal/providers/docker ./internal/runtime -run "ListDesired|StartupReconcile|DockerSourceUsesConfiguredEndpoint" -count=1</automated>
  </verify>
  <done>The repo contains a working Docker snapshot source that feeds the existing startup reconcile flow with deterministic desired records from supported Godoxy labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker API → provider | Remote container metadata is untrusted and must be filtered/normalized before becoming desired state. |
| provider → runtime startup reconcile | Source output drives later ownership-safe mutations and must remain deterministic and narrow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | internal/providers/docker/source.go | mitigate | Filter to eligible supported containers only and preserve stable source lineage via container ID. |
| T-03-04 | Denial of Service | internal/providers/docker/source.go | mitigate | Keep snapshot-only behavior, deterministic ordering, and conservative skips for unsupported/excluded cases. |
| T-03-SC | Tampering | package installs | accept | No package-manager changes are part of this plan. |
</threat_model>

<verification>
- `mise exec -- go test ./internal/providers/docker -run 'ListDesired' -count=1`
- `mise exec -- go test ./internal/runtime -run 'StartupReconcile|DockerSourceUsesConfiguredEndpoint' -count=1`
- `mise exec -- go test ./internal/providers/docker ./internal/runtime -count=1`
</verification>

<success_criteria>
- `internal/providers/docker/source.go` no longer stubs `ListDesired`.
- Real Docker snapshots emit deterministic desired records for the supported label subset only.
- Startup full reconciliation still runs through the existing runtime path with no new orchestration layer.
- Source lineage uses Docker container ID and display name consistently for all derived records.
</success_criteria>

<output>
After completion, create `.planning/phases/03-docker-godoxy-snapshot-automation/03-02-SUMMARY.md`
</output>
